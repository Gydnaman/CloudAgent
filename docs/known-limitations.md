# 已知验证限制

阶段 A 的代码评审已通过，以下两项是尚未执行的真实环境集成验证，不代表已验证完成：

1. **浏览器断开 SSE 后的端到端对账**：后端流生成器断连取消已有单元测试；尚未在真实浏览器中断开 HTTP 流，并验证 UI 随后按 run ID 查询持久事件。
2. **服务重启后的孤儿运行对账**：对账函数将持久化的孤儿运行标记为失败已有单元测试；尚未保留真实 PostgreSQL 在途运行记录、重启 API/PostgreSQL，再验证启动流程完成对账。

完整验收范围见[阶段 A 技术方案 §12](technical-proposals/phase-a.md)；实现评审记录见[此处](reviews/implementation-review.md)。
